Mock and Stage container
    *Mock
        -Docker files to stand up a Postgres v 16 DB with data injected to it. 
        -Dependency is the current .env file to be in the same directory as Dockerfile
        -Look at how to run to refresh yourself on the Windows command
    *Stage
        -Here we add more file structure and we use a Docker compose file to stand up our DB
        -Same Dependency of .env file to be in the same directory as the docker-compose.yml file.
        
            