#Make sure all the files are here
00_schema.sql
01_load_from_csv.sql
tacoma.csv
.env 


on terminal make sure we are in the directory
[repo]/Docker/Stage Container/postgres/init

#run 

docker compose up -d

#very helpful command to make sure the image is refreshed
docker compose build --no-cache postgres

# Stand down composer
docker compose down -v

in the docker compose file 
we had the value 
venue-pg-data:/var/lib/postgresql

in this instance to run it was changed to 
pgdata:/var/lib/postgresql/data
Decided to keep the venue-pg-data instead